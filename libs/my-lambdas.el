;;;;;;;; pretty lambdas and stuff -- mostly swiped from emacs wiki ;;;;;;;;;;;;

(require 'pretty-lambdada)

(pretty-lambda-for-modes)

(require 'big-lambda)

;; we choose pretty-lambda for now

;; (mapc (lambda (mode)
;;         (add-hook mode 'big-lambda-mode-turn-on)) pretty-lambda-auto-modes)

